[
  {
    "essential": true,
    "memory": 256,
    "image": "433610389961.dkr.ecr.us-west-2.amazonaws.com/consul-agent:latest",
    "name": "consul-agent",
    "dnsServers": [
      "${DNS_SERVER_1}",
      "${DNS_SERVER_2}",
      "${DNS_SERVER_3}"
    ],
    "environment": [
      {
        "name": "CONSUL_ENCRYPT",
        "value": "${ENCRYPTION}"
      },
      {
        "name": "CONSUL_DATACENTER",
        "value": "${DATACENTER}"
      }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "shared-production-infra",
        "awslogs-region": "us-west-2",
        "awslogs-stream-prefix": "consul-agent"
      }
    },
    "dockerLabels": {
      "app_name": "consul-agent",
      "app_env": "production",
      "app_region": "${AWS_REGION}"
    }
  }
]