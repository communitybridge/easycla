# Copyright The Linux Foundation and each contributor to CommunityBridge.
# SPDX-License-Identifier: MIT

type: object
x-nullable: false
title: CLA Groups
description: A list of CLA Groups
properties:
  lastKeyScanned:
    type: string
  pageSize:
    type: integer
  resultCount:
    type: integer
    format: int64
    x-omitempty: false
  projects:
    type: array
    x-omitempty: false
    items:
      $ref: '#/definitions/cla-group'
