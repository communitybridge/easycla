{
  "name": "project",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "install-frontend": "./scripts/install-frontend.sh",
    "install-backend": "./scripts/install-backend.sh",
    "install-aws-profile": "./scripts/install-aws-profile.sh",
    "prettier": "prettier --check '**/*.{ts,js,css,html}' --loglevel 'silent'"
  },
  "dependencies": {
    "graceful-fs": "^4.2.2",
    "serverless": "^1.52.2"
  },
  "devDependencies": {
    "husky": "^3.1.0"
  }
}
