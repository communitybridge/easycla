[
  {
    "essential": true,
    "image": "643009352547.dkr.ecr.us-west-2.amazonaws.com/cla-corporate-console/production:${build_hash}",
    "memoryReservation": 512,
    "name": "cla-console",
    "portMappings": [{
      "containerPort": 80
    }],
    "dnsServers": [
      "${DNS_SERVER_1}",
      "${DNS_SERVER_2}",
      "${DNS_SERVER_3}"
    ],
    "environment": [
      {
        "name": "APP_DOMAINS",
        "value": "${APP_DOMAINS}"
      },
      {
        "name": "SERVICE_80_NAME",
        "value": "CLA_CONSOLE_${build_hash}"
      },
      {
        "name": "SERVICE_80_TAGS",
        "value": "${TAG_REGION},${TAG_VPC_ID}"
      }
    ],
    "logConfiguration": {
      "logDriver": "gelf",
      "options": {
        "gelf-address": "udp://${TAG_REGION}.logstash.service.consul:12201",
        "labels": "realm,application,version,environment,region,tags"
      }
    },
    "dockerLabels": {
      "realm": "cla-corporate",
      "application": "console",
      "version": "${build_hash}",
      "environment": "production",
      "region": "${TAG_REGION}",
      "tags": "application, cla-corporate-console, ionic"
    }
  }
]