"""
Rename this file to cla_config.py and put it at the root of your CLA system application in order to
use it as your default configuration. It will override any configuration options found in
the default CLA system configuration file located at cla/config.py.
"""

import logging

LOG_LEVEL = logging.DEBUG

DEBUG = True # Debug on for development purposes.

BASE_URL = 'https://your.cla.domain.com' # Base URL used for callbacks and OAuth2 redirects.
# Default callback once signature is completed - a custom endpoint can be provided if needed.
SIGNED_CALLBACK_URL = BASE_URL + '/v1/signed'

# Define the database we are working with.
DATABASE = 'DynamoDB' # TODO: Should use in-memory database as default.
DATABASE_HOST = 'http://localhost:8000'

# Define the key-value store used.
KEYVALUE = 'Memory'
KEYVALUE_HOST = ''

# DynamoDB-specific configurations - this is applied to each table.
DYNAMO_REGION = 'us-west-2'
DYNAMO_WRITE_UNITS = 1
DYNAMO_READ_UNITS = 1

# Define the signing service to use.
SIGNING_SERVICE = 'DocuSign'
DOCUSIGN_ROOT_URL = 'https://demo.docusign.net/restapi/v2' # This is the demo endpoint.
DOCUSIGN_USERNAME = '' # DocuSign Email or UUID.
DOCUSIGN_PASSWORD = '' # Account Password.
DOCUSIGN_INTEGRATOR_KEY = '' # Integrator Key UUID.

# Repository settings.
AUTO_CREATE_REPOSITORY = True # Create repository in database automatically on webhook.

# GitHub Repository Service.
GITHUB_USERNAME = '' # Your CLA System GitHub Account or Organization Username.
GITHUB_TOKEN = '' # Personal Access Token For GitHub Account/Organization.
GITHUB_CLIENT_ID = '' # OAuth Application For CLA System.
GITHUB_SECRET = '' # OAuth Application For CLA System.
GITHUB_OAUTH_AUTHORIZE_URL = 'https://github.com/login/oauth/authorize' # Should probably stay as-is.
GITHUB_OAUTH_TOKEN_URL = 'https://github.com/login/oauth/access_token' # Should probably stay as-is.
GITHUB_PR_NOTIFICATION = 'status+comment' # 'status', 'comment', or 'status+comment'

# GitLab Repository Service.
GITLAB_DOMAIN = 'http://10.0.0.1' # URL to GitLab instance.
GITLAB_TOKEN = '' # Personal access token for the CLA user.
GITLAB_CLIENT_ID = '' # OAuth Application For CLA System.
GITLAB_SECRET = '' # OAuth Application For CLA System.
GITLAB_OAUTH_AUTHORIZE_URL = 'http://10.0.0.1/oauth/authorize'
GITLAB_OAUTH_TOKEN_URL = 'http://10.0.0.1/oauth/token'
GITLAB_MR_NOTIFICATION = 'status+comment' # 'status', 'comment', or 'status+comment'

# Email Service.
EMAIL_SERVICE = 'SMTP' # SMTP is useful for testing with MailHog or the like.
EMAIL_ON_AGREEMENT_APPROVED = True # Email user when agreement has been approved.

# SMTP Configuration.
SMTP_SENDER_EMAIL_ADDRESS = 'donotreply@cla.system'
SMTP_HOST = 'localhost'
SMTP_PORT = '25'

# AWS SES Configuration.
SES_SENDER_EMAIL_ADDRESS = 'donotreply@cla.system' # Must be verified in the SES service.
SES_REGION = 'us-east-1' # The AWS region out of which the emails will be sent.
SES_ACCESS_KEY = '' # AWS Access Key ID for the SES service.
SES_SECRET_KEY = '' # AWS Secret Access Key for the SES service.

# Storage service.
STORAGE_SERVICE = 'LocalStorage'

# Local Storage Configuration.
LOCAL_STORAGE_FOLDER = '/tmp/cla' # Storage of uploaded agreements - change to persistent location.

# S3 Storage Configuration.
S3_ACCESS_KEY = '' # AWS Access Key ID for the S3 service.
S3_SECRET_KEY = '' # AWS Secret Access Key for the S3 service.
S3_BUCKET = 'cla-system-bucket-name' # AWS S3 bucket used to store CLA files - must be unique.
