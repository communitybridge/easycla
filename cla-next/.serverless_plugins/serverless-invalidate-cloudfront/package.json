{
  "name": "serverless-invalidate-cloudfront",
  "version": "1.0.0",
  "description": "Serverless plugin that automatically performs cloudfront invalidation on deploy.",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "pretest": "yarn install",
    "test": "./node_modules/.bin/jasmine --config=jasmine.json"
  },
  "dependencies": {
    "aws-sdk": "^2.214.1"
  },
  "devDependencies": {
    "jasmine": "^3.1.0"
  }
}
